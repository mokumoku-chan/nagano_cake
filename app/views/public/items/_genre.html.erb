
      <div class = "col-md-2">
      <table class = "table table-borderless border">
        <tr>
          <th class = "border">ジャンル検索</th>
        </tr>
        <% genres.each do |genre| %>
          <tr>
            <td>
              <%= link_to genre.name, items_path(genre_id: genre.id), class: "text-dark" %>
            </td>
          </tr>
        <% end %>
      </table>
    </div>