<div class = "container">

  <h4>ショッピングカート</h4>

  <% tax = 1.1 %>

  <% @cart_items.each do |cart_item| %>


    <table class = "table">
      <tr>
        <th>商品名</th>
        <th>単価(税込)</th>
        <th>数量</th>
        <th>小計</th>
        <th></th>
      </tr>

      <tr>
        <td><%= cart_item.item.name %></td>
          <% include_tax = cart_item.item.price * tax %>
        <td><%= include_tax.floor.to_s(:delimited) %></td>

        <%= form_with model:cart_item, url: cart_item_path(cart_item), local:true do |f| %>
          <td>
            <%= f.select :amount, *[1..5]  %>
            <%= f.submit "変更", class: "btn btn-success" %>
          </td>
        <% end %>

        <td><%= (include_tax * cart_item.amount).floor.to_s(:delimited) %></td>
        <td><%= link_to "削除", cart_item_path(cart_item), method: :delete, "data-confirm" => "本当に削除しますか？" , class: "btn btn-danger" %></td>
      </tr>

    </table>

  <% end %>

</div>